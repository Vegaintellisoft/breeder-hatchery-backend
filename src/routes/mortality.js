// This file kept for backward compatibility
// New mortality routes are in mortalityCull.js
// Redirecting to mortalityCull routes

const express = require('express');
const router  = express.Router();
module.exports = router;
